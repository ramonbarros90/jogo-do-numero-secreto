/* const produtos = [
    {nome: 'Camisa', preco: 25.0, quantidade: 3},
    {nome: 'Calça', preco: 50.0, quantidade: 2},
    {nome: 'Sapato', preco: 80.0, quantidade: 1}
];

const precoTotal =  produtos.reduce((total, produto) => total + (produto.preco * produto.quantidade), 0);

const nomesProdutos = produtos.map(produto => produto.nome);
const listaProdutos = nomesProdutos.reduce((lista, nome) => lista.concat(nome), ''); */

/* const calcularSoma = (array) => {
    let soma = 0;
    array.forEach((elemento) => {
        soma += elemento;
    })
    return soma;
}

const numeros = [1, 2, 3, 4, 5];

const resultado = calcularSoma(numeros);
console.log("A soma dos elementos array é: ", resultado); */

for (let i = 30; i >= 1; i-- ){
    console.log(i)
}